<?xml version="1.0" encoding="UTF-8"?>
<!--
  The contents of this file are subject to the terms of the Common Development and
  Distribution License (the License). You may not use this file except in compliance with the
  License.

  You can obtain a copy of the License at legal/CDDLv1.0.txt. See the License for the
  specific language governing permission and limitations under the License.

  When distributing Covered Software, include this CDDL Header Notice in each file and include
  the License file at legal/CDDLv1.0.txt. If applicable, add the following below the CDDL
  Header, with the fields enclosed by brackets [] replaced by your own identifying
  information: "Portions Copyright [year] [name of copyright owner]".

  Copyright 2015-2016 ForgeRock AS.
  Portions Copyright 2018-2026 3A Systems, LLC
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.openidentityplatform.opendj</groupId>
        <artifactId>opendj-msi</artifactId>
        <version>5.1.2-SNAPSHOT</version>
    </parent>

    <artifactId>opendj-msi-standard</artifactId>
    <packaging>pom</packaging>
    <name>OpenDJ MSI Standard Package</name>

    <description>
        This module generates an OpenDJ MSI package using the WiX Toolset v5 .NET tool
        (cross-platform; no wine). The `wix` tool must be on the PATH (CI installs it via
        `dotnet tool install --global wix`).
    </description>

    <properties>
        <msi.resources>${basedir}/resources/msi</msi.resources>
        <package.dir>${project.build.directory}/${product.name.lowercase}</package.dir>
        <staging.dir>${project.build.directory}/msi-staging</staging.dir>
        <msi.file>${project.build.directory}/${product.name.lowercase}-${project.version}.msi</msi.file>
        <wix.version>5.0.2</wix.version>
    </properties>

    <dependencies>
    </dependencies>
    <build><finalName>${project.groupId}.${project.artifactId}</finalName>
        <plugins>
            <!-- Provides parsedVersion.{major,minor,incremental}Version (configured in the root pom). -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>attach-msi-and-bundle</id>
                        <phase>package</phase>
                        <goals>
                            <goal>attach-artifact</goal>
                        </goals>
                        <configuration>
                            <artifacts>
                                <artifact><file>${msi.file}</file><type>msi</type></artifact>
                            </artifacts>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Unpacks the opendj-server-legacy distribution (unpack-archive execution inherited
                 from the opendj-packages parent pluginManagement). -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
            </plugin>

            <!-- Stage the payload to harvest: keep Windows files, drop macOS *.app and Unix scripts. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <id>stage-msi-payload</id>
                        <phase>package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <delete dir="${staging.dir}"/>
                                <mkdir dir="${staging.dir}"/>
                                <copy toDir="${staging.dir}">
                                    <fileset dir="${package.dir}">
                                        <exclude name="QuickSetup.app/**"/>
                                        <exclude name="Uninstall.app/**"/>
                                        <exclude name="bin/**"/>
                                        <exclude name="setup"/>
                                        <exclude name="uninstall"/>
                                        <exclude name="upgrade"/>
                                        <exclude name="**/*.sh"/>
                                    </fileset>
                                </copy>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Build the x64 MSI with the WiX v5 .NET tool (native, no wine). -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>wix-build-msi</id>
                        <phase>package</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <executable>wix</executable>
                            <workingDirectory>${project.build.directory}</workingDirectory>
                            <arguments>
                                <argument>build</argument>
                                <argument>${msi.resources}/package.wxs</argument>
                                <argument>-arch</argument><argument>x64</argument>
                                <argument>-ext</argument><argument>WixToolset.UI.wixext</argument>
                                <argument>-bindpath</argument><argument>${msi.resources}</argument>
                                <argument>-d</argument><argument>name=${product.name}</argument>
                                <argument>-d</argument><argument>major=${parsedVersion.majorVersion}</argument>
                                <argument>-d</argument><argument>minor=${parsedVersion.minorVersion}</argument>
                                <argument>-d</argument><argument>point=${parsedVersion.incrementalVersion}</argument>
                                <argument>-d</argument><argument>staging=${staging.dir}</argument>
                                <argument>-o</argument><argument>${msi.file}</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
