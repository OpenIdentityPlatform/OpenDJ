<?xml version="1.0" encoding="UTF-8"?>
<!--
  The contents of this file are subject to the terms of the Common Development and
  Distribution License (the License). You may not use this file except in compliance with the
  License.

  You can obtain a copy of the License at legal/CDDLv1.0.txt. See the License for the
  specific language governing permission and limitations under the License.

  When distributing Covered Software, include this CDDL Header Notice in each file and include
  the License file at legal/CDDLv1.0.txt. If applicable, add the following below the CDDL
  Header, with the fields enclosed by brackets [] replaced by your own identifying
  information: "Portions Copyright [year] [name of copyright owner]".

  Copyright 2015-2016 ForgeRock AS.
  Portions Copyright 2018-2026 3A Systems, LLC
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.openidentityplatform.opendj</groupId>
        <artifactId>opendj-msi</artifactId>
        <version>5.2.0-SNAPSHOT</version>
    </parent>

    <artifactId>opendj-msi-standard</artifactId>
    <packaging>pom</packaging>
    <name>OpenDJ MSI Standard Package</name>

    <description>
        This module generates an OpenDJ MSI package using the WiX Toolset v5 .NET tool
        (Windows only: WiX's build task P/Invokes msi.dll). The `wix` tool must be on the
        PATH with the UI extension installed:
          set DOTNET_ROLL_FORWARD=Major
          dotnet tool install --global wix --version 5.0.2
          wix extension add -g WixToolset.UI.wixext/5.0.2
        The module itself is part of every reactor so that the release plugin keeps its
        version in step with the rest of the build; the toolchain profile below is what
        turns the MSI build on, and only when %USERPROFILE%\.dotnet\tools\wix.exe exists.
    </description>

    <properties>
        <msi.resources>${basedir}/resources/msi</msi.resources>
        <package.dir>${project.build.directory}/${product.name.lowercase}</package.dir>
        <!-- Exclusions are done here (Ant), not in WiX <Files> (which only takes Include in WiX 5):
             stagingRoot = payload minus lib; stagingLib = lib minus the Unix scripts. -->
        <staging.root>${project.build.directory}/msi-staging</staging.root>
        <staging.lib>${project.build.directory}/msi-staging-lib</staging.lib>
        <msi.file>${project.build.directory}/${product.name.lowercase}-${project.version}.msi</msi.file>
    </properties>

    <dependencies>
    </dependencies>

    <!-- Everything that needs Windows lives in this profile rather than in the module list
         that pulls the project in: a module outside the reactor is a module the release
         plugin cannot version-bump (see the note in opendj-packages/pom.xml). Off, the
         module is a pom that builds nothing; on, it produces the MSI. The activation is the
         one the packages pom used to carry - Windows plus an installed WiX v5 .NET tool,
         both conditions required - so a plain `mvn install` on a contributor's Windows
         machine without wix no longer fails mid-reactor with "Cannot run program wix".
         Name it explicitly (-Pdistribution-windows-msi) when wix lives outside the dotnet
         global-tools directory; release.yml does exactly that. -->
    <profiles>
      <profile>
        <id>distribution-windows-msi</id>
        <activation>
            <os><family>windows</family></os>
            <file><exists>${env.USERPROFILE}/.dotnet/tools/wix.exe</exists></file>
        </activation>
    <build><finalName>${project.groupId}.${project.artifactId}</finalName>
        <plugins>
            <!-- Unpacks the opendj-server-legacy distribution (unpack-archive execution inherited
                 from the opendj-packages parent pluginManagement). -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
            </plugin>

            <!-- Stage the payload to harvest: keep Windows files, drop macOS *.app and Unix scripts. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <id>stage-msi-payload</id>
                        <phase>package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <!-- Everything except lib (and macOS/Unix bits) -> stagingRoot -->
                                <delete dir="${staging.root}"/>
                                <mkdir dir="${staging.root}"/>
                                <copy toDir="${staging.root}">
                                    <fileset dir="${package.dir}">
                                        <exclude name="QuickSetup.app/**"/>
                                        <exclude name="Uninstall.app/**"/>
                                        <exclude name="bin/**"/>
                                        <exclude name="lib/**"/>
                                        <exclude name="setup"/>
                                        <exclude name="uninstall"/>
                                        <exclude name="upgrade"/>
                                        <exclude name="**/*.sh"/>
                                    </fileset>
                                </copy>
                                <!-- lib (the Windows launchers included) -> stagingLib -->
                                <delete dir="${staging.lib}"/>
                                <mkdir dir="${staging.lib}"/>
                                <copy toDir="${staging.lib}">
                                    <fileset dir="${package.dir}/lib">
                                        <exclude name="**/*.sh"/>
                                    </fileset>
                                </copy>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Build the x64 MSI with the WiX v5 .NET tool (exec-maven-plugin version
                 managed in the root pom). -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>wix-build-msi</id>
                        <phase>package</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <executable>wix</executable>
                            <workingDirectory>${project.build.directory}</workingDirectory>
                            <arguments>
                                <argument>build</argument>
                                <argument>${msi.resources}/package.wxs</argument>
                                <argument>-arch</argument><argument>x64</argument>
                                <argument>-ext</argument><argument>WixToolset.UI.wixext</argument>
                                <argument>-bindpath</argument><argument>${msi.resources}</argument>
                                <argument>-d</argument><argument>name=${product.name}</argument>
                                <argument>-d</argument><argument>major=${parsedVersion.majorVersion}</argument>
                                <argument>-d</argument><argument>minor=${parsedVersion.minorVersion}</argument>
                                <argument>-d</argument><argument>point=${parsedVersion.incrementalVersion}</argument>
                                <argument>-d</argument><argument>stagingRoot=${staging.root}</argument>
                                <argument>-d</argument><argument>stagingLib=${staging.lib}</argument>
                                <argument>-o</argument><argument>${msi.file}</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Attach the MSI produced by the wix execution above. Declared after it so the
                 attachment happens once the file exists: `target` is not cleaned between runs,
                 and attaching first would silently pick up a stale MSI if the wix build were
                 skipped. -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>attach-msi</id>
                        <phase>package</phase>
                        <goals>
                            <goal>attach-artifact</goal>
                        </goals>
                        <configuration>
                            <artifacts>
                                <artifact><file>${msi.file}</file><type>msi</type></artifact>
                            </artifacts>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
      </profile>
    </profiles>
</project>
